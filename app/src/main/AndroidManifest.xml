<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EdifyHub"
        tools:targetApi="31">

        <!-- ✅ Set CreateQuizActivity as launcher -->
        <activity
            android:name=".teachercreatequiz.CreateQuizActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".AdminAproveTeacher.AdminApprovalActivity" />
        <activity android:name=".teacherProfileManage.TeacherProfileActivity" />
        <activity android:name=".teacherProfileManage.ManageInstituteActivity" />

    </application>
</manifest>
